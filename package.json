{
  "name": "marketing-os",
  "version": "0.0.0-sprint-0",
  "private": true,
  "description": "Marketing OS Sprint 0 runnable backend baseline.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "db:migrate": "node scripts/db-migrate.js",
    "db:seed": "node scripts/db-seed.js",
    "openapi:lint": "node scripts/openapi-lint.js",
    "test": "node --test test/*.test.js",
    "test:integration": "node --test test/integration/*.test.js",
    "verify": "node scripts/verify-sprint0.js && npm run openapi:lint && npm test && npm run test:integration && npm run db:seed && npm run db:migrate"
  }
}
